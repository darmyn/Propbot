from dis import dis
import os
import replit
import disnake
import bot

botToken = os.getenv("botToken")
steamToken = os.getenv("steamToken")
youtubeToken = os.getenv("youtubeToken")

bot.main().run(os.getenv("botToken"))


